#include "main.h"
/**
 *main - tests if Positive_or_negative is functioning, function checks if integers are + or -
 * Return: 0
 */
int main(void)
{
int i;
i = 0;
positive_or_negative(i);
return (0);
}
