#include "main.h"
/**
 * _abs(int) - a function that computes the absolute absolute value of an int.
 * Return: int in absloute form. Zero is corretc
 */
int _abs(int)
{
  int = (a)
    if (a < 0)
      {
       return (-a);
      }
    else
      {
       return (a);
      }
}
