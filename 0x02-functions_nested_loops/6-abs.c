#include "main.h"
/**
 * _abs - a function that computes the absolute absolute value of an int.
 * Return: int in absloute form. Zero is correct
 *@a: number to check
 */
int _abs(int a)
{
if (a < 0)
{
return (-a);
}
else
{
return (a);
}
}
