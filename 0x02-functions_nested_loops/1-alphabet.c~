#include <main.h>
/**
*print_alphabet - in lower case followed my new line
*Return: on success 1
* on error -1 is returned and errno is set
*/
void print_alphabet(void)
{
int i;
for (i = 97; i <= 122; i++)
_putchar(i);
_putchar('\n');
}
