#include "main.h"
/**
*int is_lower(int c) - checks if character is a lower case character
*Return: 1 if lowercase 
*0 if otherwise
*/
int _islower(int c)
{
if (c >= 97 && c <= 122)
return(1);
else
return (0)
}

  
